CREATE TABLE
    Insights (
        insightID INTEGER NOT NULL AUTO - INCREMENT,
        json TEXT,
        domain TEXT,
        savedAt DATETIME DEFAULT CURRENT_TIMESTAMP
        -- id, timestamp, domain(sales/hr/etc.) and json (or simply text) fields.
    );